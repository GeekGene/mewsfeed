{
  "name": "tests",
  "private": true,
  "scripts": {
    "types": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:watch:pattern": "set -x && vitest watch --testNamePattern ${PATTERN}"
  },
  "dependencies": {
    "@holochain/client": "^0.14.1",
    "@holochain/tryorama": "^0.11.2",
    "@msgpack/msgpack": "^2.8.0",
    "typescript": "^4.9.4",
    "vitest": "^0.28.5"
  },
  "type": "module",
  "devDependencies": {
    "@types/ws": "^8.5.5"
  }
}
